# Validation results

Updated on 2026-07-19 (eleventh pass) for the Inkjet setup screen rework: intro blurb, full provider list with disabled entries, and the no-CLIs state.

- Setup screen: a one-line intro now sits above the provider section ("Inkjet is Inktile's built-in AI agent: it writes right into this document, connecting automatically to the AI CLIs installed and signed in on this machine."). The provider list always shows every provider (Claude, Codex, OpenCode) with its status detail; unavailable ones render greyed out (`.is-unavailable`, opacity .45, no hover) with their radio disabled, instead of being hidden. When no provider is available at all, the list is replaced by a single note — "No AI CLIs are available — install and sign in to Claude, Codex, OpenCode, then check again." — plus the Check again button (the old per-provider error breakdown is gone).
- `npm run check` (docs, build, archive round trip, broker typecheck, smoke syntax): passed. `CHROMIUM_PATH="$LOCALAPPDATA/ms-playwright/chromium-1223/chrome-win64/chrome.exe" npm run test:ui`: passed end to end with reworked provider-screen assertions — intro present, all three providers listed, exactly two disabled, the disabled card computed at sub-0.6 opacity, and the single available provider enabled and preselected.
- `npm run release:desktop`: not run — `Inktile.exe` is still open (active app session from 11:39 PM, not force-closed), which blocks the root-artifact refresh. Root `Inktile.exe` and the portable ZIP remain on the seventh-pass build; rerun `npm run release:desktop` with Inktile closed to ship the eighth-through-eleventh-pass panel work.

Updated on 2026-07-19 (tenth pass) for the browser build's Inkjet desktop-only note.

- Browser behavior: opening the Inkjet panel in a plain browser tab now shows a single muted note — "Inkjet is a desktop-only feature." — and nothing else: no connect attempt, no error text, no Retry button, no provider details. A new `agentSupportedHere()` helper in `src/agent/connection.ts` (desktop shell or test-mock transport present) gates both the auto-connect effect and the setup screen in `InkjetPanel.tsx`; desktop and mocked-test behavior are unchanged.
- `npm run check` (docs, build, archive round trip, broker typecheck, smoke syntax): passed. `CHROMIUM_PATH="$LOCALAPPDATA/ms-playwright/chromium-1223/chrome-win64/chrome.exe" npm run test:ui`: passed end to end, including a new plain-browser section (panel opened before the mock is injected must show the desktop-only note with zero buttons in the setup pane) followed by the unchanged mocked-broker coverage. These runs also validated the concurrently landed OpenCode backend registration and ninth-pass sizing state they built on.
- `npm run release:desktop`: preflight, optimized build (1m 38s), and MSI + NSIS bundles passed, but the root-artifact copy failed again — `Inktile.exe` was relaunched mid-build (an active app session, not force-closed). The MSI/NSIS installers under `src-tauri/target/release/bundle/` are current; root `Inktile.exe` and the portable ZIP remain on the seventh-pass build. Rerun `npm run release:desktop` with Inktile closed to refresh them.

Updated on 2026-07-19 (ninth pass) for Inkjet pixel-aware sizing: media auto-fit on insert and real tile geometry in `read_document`.

- Sizing: `insert_media` now decodes image/video bytes (blob-URL measurement with a 3s timeout) and sizes the new row to the media's aspect ratio at full document width, clamped 120–1600px, falling back to the old flat 420px only when the bytes cannot be decoded. `read_document` now reports the document `pageWidth`, every tile's rendered `widthPx` (pageWidth × row share), and each image/video asset's intrinsic `width`/`height` (measured once per asset and cached). Both backend system prompts gained a sizing bullet (pixel geometry, "contain" letterboxing, re-fit rows after narrowing a tile, leave text rows unsized), and the `read_document`/`set_row_height`/`create_image`/`fetch_media` tool descriptions now state the pixel contract. Broker protocol untouched except additive snapshot fields; no persisted-format changes.
- `npm run check` (docs, build, archive round trip, broker typecheck, smoke syntax): passed. `CHROMIUM_PATH="$LOCALAPPDATA/ms-playwright/chromium-1223/chrome-win64/chrome.exe" npm run test:ui`: passed end to end with new assertions — the scripted agent turn inserts a 200×100 SVG, and `read_document` must report `pageWidth` > 0, `widthPx` equal to `pageWidth` for the full-row tile, intrinsic `200x100`, and an auto-fitted row height of exactly `round(pageWidth / 2)`.
- Suite repair along the way: the smoke test's wordmark locator still expected `src="/inktile-logo.png"`, but commit `a8d9be4` (GitHub Pages relative asset paths) changed the app to `./inktile-logo.png`, so the suite failed at boot before any of this pass's changes — pre-existing breakage, fixed by updating the locator.
- `npm run release:desktop`: not run in this pass — `Inktile.exe` is running (an active app session, not force-closed), which blocks the root-artifact refresh. The shipped artifacts still carry the eighth-pass-noted state; rerun `npm run release:desktop` with Inktile closed to ship the sizing behavior (the broker/prompt halves live in `agent/*.mjs`, which the shipped app loads from disk, but `applyOp.ts`/`protocol.ts` are bundled and need the rebuild).

Updated on 2026-07-19 (eighth pass) for the Inkjet typewriter reveal and the one-sentence session helptext.

- Typewriter: all model text in the Inkjet panel — the ephemeral thinking bubble and the persisted answer card — now reveals a few characters per frame (minimum 3, accelerating with the backlog so even a whole answer arriving at once finishes in well under a second) instead of snapping whole chunks in. A new `InkjetTyped` component in `InkjetPanel.tsx` drives the reveal via `requestAnimationFrame`, re-scrolls the transcript and the capped thinking bubble on every advance, and remembers finished entries (`typedRef`) so reopening the panel renders them instantly instead of retyping. The chat empty state was reduced to one sentence: "Ask Inkjet to write in this inktile — one Ctrl+Z undoes its whole turn."
- `npm run check` (docs, build, archive round trip, broker typecheck, smoke syntax): passed. `CHROMIUM_PATH="$LOCALAPPDATA/ms-playwright/chromium-1223/chrome-win64/chrome.exe" npm run test:ui`: passed end to end; the Inkjet section now waits for typed text instead of sampling it (mid-turn process note fully revealed in the thinking bubble with no answer card present; slow-turn reasoning phrase fully revealed) and adds a mid-reveal assertion — the answer card must be observable with partial text before the full answer is on screen.
- `npm run release:desktop`: not run in this pass — `Inktile.exe` was open (an active app session, not force-closed), which blocks the root-artifact refresh. The shipped artifacts still carry the seventh-pass build; rerun `npm run release:desktop` with Inktile closed to ship the typewriter behavior.

Updated on 2026-07-19 (seventh pass) for separating Inkjet's in-progress output from its final answer.

- Chat differentiation: everything the model says while a turn runs — extended-thinking deltas and the interim status notes between tool calls — now streams into the ephemeral dashed "thinking" bubble, and only the turn's final answer persists in the transcript, delivered once when the backend's turn completes (it replaces the temporary bubble). The `narration` protocol message became `answer`; the Claude backend tracks message boundaries (`message_start`/`content_block_start`, with paragraph separators between blocks), takes the answer from the result payload's final text (last streamed assistant message as fallback), and the Codex backend forwards interim `agent_message` items ephemerally with the last one becoming the answer. Both system prompts now tell the model its mid-turn messages are transient and to end the turn with a brief summary. Panel-side, `appendAnswer` (one persisted markdown card per turn) replaced the chunk-merging `appendNarration`.
- `npm run check` (docs, build, archive round trip, broker typecheck, smoke syntax): passed. `CHROMIUM_PATH="$LOCALAPPDATA/ms-playwright/chromium-1223/chrome-win64/chrome.exe" npm run test:ui`: passed end to end with new assertions — mid-turn, the scripted process note renders only in the ephemeral thinking bubble and no `.inkjet-entry--agent` card exists; after turn-end, the answer card renders (markdown bold + list) and the thinking bubble is gone; the existing dashed-frame styling and stop-clears-thinking assertions still pass.
- Real-CLI end-to-end (broker driven over stdio, one tiny Haiku turn on this machine's login): 373 chars of in-progress output arrived on the `thinking` channel, exactly one `answer` message carried the final text, zero legacy `narration` messages, turn ended `done`.
- `npm run release:desktop`: preflight and optimized build passed (1m 40s), MSI and NSIS bundles were produced, but the root-artifact refresh failed because `Inktile.exe` was running (in use by an open app session, not force-closed). Root `Inktile.exe` and the portable ZIP therefore still carried the sixth-pass build at that point.
- Follow-up (same day): the README was refreshed — `flower-screenshot.png` (the Inkjet flower-drawing session) replaced `inktile-preview.png` as the hero image, and a new "Inkjet, the built-in agent" section documents the zero-setup provider detection, document control, turn lock, thinking-versus-answer transcript model, single-undo revert, and local broker containment, linking to Architecture for mechanics; `npm run check:docs` passed. With Inktile closed, the `npm run release:desktop` rerun then passed fully — preflight, optimized build, MSI + NSIS bundles, root executable copy, portable ZIP recreation, and hashes. Root `Inktile.exe` exactly matches `src-tauri/target/release/inktile.exe`, so all artifacts now carry the seventh-pass build.
- A further user-initiated `npm run release:desktop` rerun (same day, same seventh-pass source) passed fully again and refreshed every artifact; rebuilds are not byte-identical (patched bundle-type metadata and re-zipping), so the hashes below supersede the previous rerun's. Root `Inktile.exe` re-verified to exactly match `src-tauri/target/release/inktile.exe`.
- Release SHA-256: `Inktile.exe` `DC3D27FAAFCE221A59ADAABCC96815C3F98F885F47E93B0EBEA293689930876A`; portable ZIP `40B6501E8722A76B499052CD39C4B0F42FD934FFD860F6858429B8350CD5BC6A`; MSI `2DD3E14553242F6329183368824B082C043FBC7B21D63D56510924E4A446F6DE`; NSIS `428E252AF7C898FDC21150C5F7F649F5093F5360A41740AB96721E5A08AADF08`.

Updated on 2026-07-19 (sixth pass) for the Gemini backend removal, desktop rebuild, and project meta refresh (README, docs, skill, agent instructions, check gate).

- Gemini removal: the user reverted the short-lived Gemini backend (Google discontinued Gemini CLI / Code Assist for individuals in favor of Antigravity; the installed CLI fails auth with `IneligibleTierError`), and this pass cleaned up the revert's leftovers — the dangling `backend-gemini.mjs` import in `agent/broker.mjs` (which crashed the broker on spawn, disabling all of Inkjet in the shipped app), the backend table/status/reset entries there, `"gemini"` in `src/agent/protocol.ts` and `InkjetPanel.tsx`, the mock status line in `scripts/ui-smoke.mjs`, the Gemini passage in `docs/ARCHITECTURE.md`, and the `agent/.gemini-workspace/` directory. Inkjet supports Claude Code and Codex only.
- Check gate: `npm run check` now includes `npm run check:agent`, so the pre-commit hook and the release preflight catch a broken broker — the dangling import above had passed every existing gate because nothing in `check` compiled `agent/*.mjs`.
- Meta refresh: README gained the Export picker, library pinning/duplication, the `agent/` code-map row, and dropped PDF/print from the out-of-scope list; `PRODUCT_INVARIANTS.md`/`ARCHITECTURE.md`/`FILE_FORMAT.md` now cover library pinning, card duplication, the card-size control, and the autosave toggle (save/discard prompt when leaving with autosave off); `skills/inktile-app/SKILL.md` and `AGENTS.md` gained routing and validation entries for the library, export, and the Inkjet broker.
- `npm run release:desktop` (run after the Gemini removal and README update, before the docs/skill/gate edits, which are rebuild-exempt meta changes): passed in one run — preflight, optimized Tauri build (1m 31s), MSI + NSIS bundles, root executable copy, portable ZIP recreation, and hashes. Root `Inktile.exe` matches `src-tauri/target/release/inktile.exe`; a binary grep confirmed no Gemini strings ship in the new executable.
- `npm run check` with the new chain (docs, TypeScript/Vite production build, archive round trip, broker typecheck, UI smoke syntax): passed. `CHROMIUM_PATH="$LOCALAPPDATA/ms-playwright/chromium-1223/chrome-win64/chrome.exe" npm run test:ui`: passed end to end, including the Inkjet provider-screen assertions against the two-provider status message.
- Not covered: the UI suite has no assertions yet for library pinning, card duplication, the card-size control, or the autosave toggle — those features shipped without smoke coverage and remain a known gap.
- Release SHA-256: `Inktile.exe` `4AC7A7478775B0869A64F9876B5D53E630AE1F1A7B5D9524E954D8FD0A6DD8CE`; portable ZIP `D1AD509D94530FB5316837F1B2667751E0744577DBA6A4678B31E5E0A5A1EF1D`; MSI `5026527BCE7FF0E741EDED45B3F8DFBB048137020BDF2F9679C040601D39FABD`; NSIS `9986ED9AEBC7202F9FB7B6D9C338EB7BC4351812F919AC211FCD393747E5DD2E`.

Updated on 2026-07-19 (fifth pass) for the Inkjet chat popup rework.

- Popup: the panel's left edge drags to resize (280–760px, persisted in localStorage, UI-scale-aware pointer math); the composer textarea auto-grows with its content up to a 190px cap (manual resize grip removed, hidden overflow scrolling past the cap); the transcript and setup screens hide native scrollbars and the transcript is driven by the new `ElementScrollbar` (vertical element-bound sibling of the workspace/row overlay scrollbars); agent narration renders through `InkjetMarkdown`, a dependency-free markdown renderer that emits React elements only (paragraphs/line breaks, headings, bullet + numbered lists, blockquotes, fenced + inline code, bold, italic, links, rules), so model output cannot inject markup.
- `npm run check`, `npm run check:agent`, `npm run check:docs`: passed. `CHROMIUM_PATH="$LOCALAPPDATA/ms-playwright/chromium-1223/chrome-win64/chrome.exe" npm run test:ui`: passed end to end with new assertions for one-to-one edge-drag width growth, grip-free composer growth and shrink-back, hidden native transcript scrollbar plus a functional overlay-handle drag (constrained wrap → handle drag → scrollTop moves), and markdown narration (bold + list items rendered as elements). One suite fix along the way: the panel toggle locator became ambiguous with the resize handle's aria-label and now targets the button role.
- `npm run release:desktop`: the first run compiled and bundled but could not refresh root artifacts because `Inktile.exe` was running (in use); after closing the app, the rerun passed fully — preflight, optimized build, MSI + NSIS bundles, root executable copy, portable ZIP recreation, and hashes. Root `Inktile.exe` matches `src-tauri/target/release/inktile.exe`.
- Release SHA-256: `Inktile.exe` `6826AC5C84374B5CEB281F022B376EA3F7F84732E4363FCFC4EB86861C21A65C`; portable ZIP `42A65AA46A6A331276CBD7194227DF555A80B41E839928439BD423DE5184C96B`; MSI `C477847EE14593FBF993D79A9B32ABC4F3875ABF9129D289C46B577B9C734C6D`; NSIS `4D7A69FAE010E8101374B9B2F1D8E9426DC0D647481FA73E53D050F38EAEF06B`.

Updated on 2026-07-19 (fourth pass) for unchanged-document detection (no forced re-read every turn).

- The broker now tracks each document's revision at the end of the last cleanly finished turn and briefs every new turn as "first", "changed", or "unchanged" (also surfaced on the `turn-start` message). Unchanged turns are told to skip `read_document` and build on conversation memory; changed/first turns are told to read first, and that is enforced mechanically — the op channel is seeded with a sentinel revision so any write attempted before `read_document` is rejected by the existing guard. Stops, failures, and "New session" clear the memory. Broker-only change: the shipped app loads `agent/*.mjs` from disk, and the one `protocol.ts` addition is a type-only optional field, so no desktop rebuild was needed.
- Three-turn real-Claude validation passed: turn 1 (first) read then inserted; turn 2 (no edits, briefed unchanged) appended with **no** `read_document`; turn 3 (simulated user edit, briefed changed) attempted a stale write, was bounced by the sentinel, re-read, and completed — ops `[append_text, read_document, append_text]`.
- `npm run check`, `npm run check:agent`, and the full `test:ui` suite: passed.

Updated on 2026-07-19 (third pass) for Inkjet full document control and the audio-insertion fix.

- Full control: the tool surface now covers the whole document — `set_title`, `edit_notes` (back faces, via a new `setPageNotes` context mutation), `delete_pages`, `set_row_height` (96–1600px, shared per row), `set_row_widths` (normalized fractions, ≥8% each), `set_vertical_align`, `create_drawing`/`edit_drawing` (normalized 0..1 strokes with tool/width/opacity sanitation, append or replace), `insert_versions`/`edit_versions`/`convert_versions_to_text` (matching the version rail's own controls), alongside the existing text/page/media tools. `read_document` now returns notes, versions drafts + active index, drawing stroke counts, row heights, width fractions, and alignment. Both backend prompts state the agent has full control and that a whole turn reverts with one undo.
- Audio fix: `fetch_media` no longer trusts file extensions or first-of-set MIME fallbacks (which labeled e.g. an octet-stream .mp3 — or an HTML error page — as "audio/aac", producing unplayable assets). It now sniffs the downloaded bytes (magic numbers for MP3/AAC/WAV/OGG/Opus/FLAC/M4A/MP4/QuickTime/WebM/PNG/JPEG/GIF/WebP/BMP/AVIF/SVG), corrects mislabeled server MIME types, and rejects text/HTML masquerading as media with a clear error. A 16-case sniffer unit run passed.
- `npm run check`, `npm run check:agent`: passed. `CHROMIUM_PATH="$LOCALAPPDATA/ms-playwright/chromium-1223/chrome-win64/chrome.exe" npm run test:ui`: passed end to end; the Inkjet scripted turn now exercises rename, versions insert/rework/delete, drawing create + append, audio insertion (a real WAV asserted to render `<audio controls>` with blob type audio/wav), row resize, notes on the back face, and page deletion — all reverting with the single undo (title and page count restored).
- Real-CLI end-to-end: a Claude turn was asked to rename the document via the new tool; it called `set_title` and the title check passed.
- `npm run release:desktop`: passed (preflight, optimized build, MSI + NSIS, root artifacts, hashes). Root `Inktile.exe` matches `src-tauri/target/release/inktile.exe`.
- Release SHA-256: `Inktile.exe` `6C17D0AFBBF4D41C8F1725995C025D17063DC2F2F812EBE100C05457D667949F`; portable ZIP `EE08B231490545EA5683A4493011A8544214E450E6B573FEF67173F15A2E6F1B`; MSI `EEFCB422F340D6507F331137DD4635A227A3576122839B690C968406C6A06F19`; NSIS `AEAAE19EE7BFCE3803390A9C729DFA49A371D4D1ADFFDE221A92D741FB0187C3`.

Updated on 2026-07-19 (second pass) for the Inkjet rename and the provider/model/session flow.

- Inkjet: the agent panel is now branded Inkjet (`InkjetPanel.tsx`, `.inkjet-*` classes, "Inkjet is writing" indicator). Opening the popup shows a setup screen that lists only auto-detected providers (installed + signed in; unavailable ones are hidden, and a no-providers state explains what is missing per provider), then a model dropdown fed from the broker's status message (Claude: Default/Fable/Opus/Sonnet/Haiku aliases; Codex: Default plus current gpt-5.6 Terra/Sol/Luna and gpt-5.3-codex slugs verified against the installed CLI), then **Start session**, which resets backend resume state (new `reset` protocol message) and opens the chat view; "New session" returns to setup. The chosen model rides on each prompt (`--model`/`-m` pass-through).
- `npm run check`, `npm run check:agent`: passed. `CHROMIUM_PATH="$LOCALAPPDATA/ms-playwright/chromium-1223/chrome-win64/chrome.exe" npm run test:ui`: passed end to end; the Inkjet section now asserts unavailable providers are hidden, a single available provider is preselected, the session header shows provider · model, the prompt carries the chosen model, and New session returns to the provider screen — plus the existing lock/stream/revision/undo/stop coverage.
- Real-CLI end-to-end: Claude turn with `model: "sonnet"` passed through the broker (`--model sonnet` accepted, read → insert → append, clean exit).
- `npm run release:desktop`: the first run failed with ENOSPC — drive C: had 0 bytes free (the npm cache had grown to 6.4 GB); after deleting `%LOCALAPPDATA%\npm-cache\_cacache` (~5.9 GB freed) the rerun passed fully. Root `Inktile.exe` matches `src-tauri/target/release/inktile.exe`.
- Release SHA-256: `Inktile.exe` `ABAC431BFE7E30922C8BA4ABC6EC15D3AB6C2496D028661A1CE561FB44303BA4`; portable ZIP `582C37EF6C517479946D61EAE6C0182A97657E904C56F879F07D091F1664C60D`; MSI `22F4460FFEDA1BF951D277C67BDD3D5551A6D36FFE2038D264B33385C7DA4737`; NSIS `0642FF6FD155680866AC4A016C1EDE676940B623AB7A308F9C6EA99489861725`.

Updated on 2026-07-19 for the zero-setup in-app agent (Claude/Codex writing into the open inktile). Earlier results remain below for regression context.

- Agent: the desktop app spawns a dependency-free broker (`agent/*.mjs`, plain Node, no node_modules) via new Tauri commands (`agent_start`/`agent_send`/`agent_stop` in `src-tauri/src/lib.rs`) and bridges its stdio to the webview — no server to run, no token to pair, browser build excluded by design. The broker drives the user's installed `claude` (headless stream-json) and `codex` (`exec --json`, read-only sandbox, live web search) CLIs on their existing logins, exposing shared document tools over a hand-rolled loopback streamable-HTTP MCP endpoint (stateful sessions + explicit tool annotations — both required by the Codex rmcp client). App-side: turn lock with one-undo-per-turn, revision guard, live op streaming via `flushSync`, prompt panel with backend picker and stop.
- `npm run check`: passed (docs, TypeScript/Vite production build, archive round trip, UI smoke syntax). `npm run check:agent` (tsc checkJs over `agent/*.mjs`): passed. `cargo check` on the new Tauri commands: passed.
- `CHROMIUM_PATH="$LOCALAPPDATA/ms-playwright/chromium-1223/chrome-win64/chrome.exe" npm run test:ui`: passed end to end, including the agent section (panel auto-connects to a mocked broker transport; read-only lock + indicator during a turn; live streamed appends visible mid-turn; stale-revision write rejected; whole turn reverts with one Ctrl+Z; stop keeps partial work and still reverts as one step).
- Real-CLI end-to-end (broker driven over stdio by a simulated app, tiny prompts on this machine's own logins): Claude turn passed (read_document → insert_page → streamed append_text, token-level narration, clean exit); Codex turn passed (read_document → insert_page through the MCP endpoint, chunked narration); interrupt test passed (stop after two appends → reason "stopped", partial content kept, clean tree-kill on Windows).
- `npm run release:desktop`: passed in one run (preflight `npm run check`, optimized Tauri build, MSI + NSIS bundles, root executable copy, portable ZIP recreation, hashes after final copies). Root `Inktile.exe` matches `src-tauri/target/release/inktile.exe`.
- Release SHA-256: `Inktile.exe` `D296D9FB8614B73BACC428B6552621680641CACC4922049BE0DD36776E418035`; portable ZIP `0BE040DFFA84510586160494D8BC43DD1F2CAE60A2184607543E1820A9B13CED`; MSI `D8BB435564C0DCEEB4E7121B754D13644FF1DB2C1E6B6D2565B062706FB90150`; NSIS `8C138871A4A06F9A9A3583102A8A6DF2D7E5B7A7EDDCE7BAEEAEF1DEFD531710`.
- Not verified here: a live GUI session of the packaged app driving a real turn (the harness cannot operate the desktop window). The spawn path it uses (walk up from the exe directory to find `agent/broker.mjs`, `node` from PATH) is covered by the passing Rust build and the stdio e2e runs above.

Updated on 2026-07-19 for top-anchored text by default and multi-tile selection (Ctrl+A, Ctrl+Click handles, group drag/copy/cut/paste/duplicate/flip/delete). Earlier results remain below for regression context.

- Text anchoring: new and normalized pages default `verticalAlign` to `top`; middle and bottom remain per-page choices in the header.
- Multi-tile selection: `TileSelectionContext` owns editor selection state, an in-app tile clipboard, and tile shortcuts (Ctrl+A select all outside editable fields, Ctrl+C/X/V, Ctrl+D, Delete/Backspace, F flip, Escape clear). Ctrl+Click on a handle toggles membership; dragging a selected handle moves the whole selection (same-row tiles stay grouped, four-per-row limit enforced); right-clicking a selected tile opens a group menu, and the single-tile/background menus gained Copy and Paste. Group operations are single undoable commits via new `DocumentContext` batch mutations (`deletePages`, `duplicatePages`, `movePages`, `togglePagesSide`, `pastePages`).
- `npm run check`: passed (8 Markdown files, TypeScript/Vite production build, `.inktile` archive round trip, UI smoke syntax).
- `CHROMIUM_PATH="$LOCALAPPDATA/ms-playwright/chromium-1223/chrome-win64/chrome.exe" npm run test:ui`: passed end to end. New coverage: default top anchoring, Ctrl+A/Escape, Ctrl+Click multi-selection, Ctrl+D + Delete, Ctrl+C/Ctrl+V, the "2 tiles selected" right-click menu with group flip, the F shortcut, a two-tile group drag that keeps the tiles rowed together, and click-away clearing. The suite's library-card selectors were updated to `.inktile-card[data-inktile-id]` and empty-state "Create your first inktile" labels to match the current Home (action cards had joined the grid), and the native Tauri mock gained v2 `metadata`/`transformCallback`/event-plugin stubs required by Home's native drag-drop listener.
- Follow-up (same day): the selection ring is now inset (`outline-offset: -2px`) because flush rows/grouped cells painted their neighbor's card background over an outside ring's bottom/right edges; and a plain handle click (press and release without pointer travel past a 3px jitter threshold) collapses the selection to just that tile, while a real drag still moves the whole group. `PageView`'s stale `verticalAlign ?? "center"` fallback also moved to `top`. `npm run check` and the full `test:ui` suite (with a new collapse-on-click assertion) passed again after these changes.
- `npm run release:desktop`: the first run failed on stale generated Cargo metadata referencing the repository's old `Folio` path; after `cargo clean --release` the rerun passed preflight, optimized Tauri build, MSI and NSIS bundling, root executable copy, portable ZIP recreation, and final hashing, and the follow-up rebuild repeated that cleanly. Root `Inktile.exe` exactly matches `src-tauri/target/release/inktile.exe`.
- Second follow-up (same day): the selected tile's left handle now carries the same inset selection ring as its card (grouped-row handles sit flush at a 30px stride, so an outside ring would bleed into the neighbor). `npm run check` and the full `test:ui` suite (with a handle-outline assertion) passed, and `npm run release:desktop` refreshed all artifacts again.
- Third follow-up (same day): row edges are selectable insertion points. A plain click on a row's bottom resize strip (drags still resize; the gesture now checkpoints on first pointer travel so an edge click leaves no undo entry) selects the edge and shows an accent line; Ctrl+V pastes the tile clipboard at that edge, and right-clicking an edge opens a menu with "Add tile here" and "Paste here". Edge and tile selection are mutually exclusive. `npm run check` and the full `test:ui` suite passed with new edge-click, edge-paste position, and edge-menu assertions, and `npm run release:desktop` refreshed all artifacts.
- Fourth follow-up (same day): edges generalized to every side. The strip above the first row selects the document-top edge (fixing that paste could never target the first position), a still click on a column boundary selects the vertical edge between grouped tiles (the column-resize gesture also checkpoints on first travel now), and new strips on each row's outer left/right edges select column edges 0 and row-length that insert at the start or end of that row. Vertical edges insert into the row under the four-per-row cap: menu items disable and Ctrl+V reports "A row holds at most four tiles" when tiles will not fit. `EdgeSelection` became a row/column union backed by new `addPageAt`/`pastePagesAt` position-based mutations. `npm run check` and the full `test:ui` suite passed with new top-, between-, and side-edge click/paste/menu assertions, and `npm run release:desktop` refreshed all artifacts again. Root `Inktile.exe` exactly matches `src-tauri/target/release/inktile.exe`.
- Fifth follow-up (same day): edge-strip hover affordances now light only under the cursor itself. The row-bottom and column-boundary lines previously appeared whenever the pointer hovered anywhere over the row (`.page-row:hover` / `.page-row--multi:hover`); both rules now key off the strip's own `:hover`, matching the top and side strips, while an in-progress resize still keeps its line visible. `npm run check` and the full `test:ui` suite passed with new assertions that hovering a tile bolds no edge and hovering a strip bolds only that strip, and `npm run release:desktop` refreshed all artifacts again. Root `Inktile.exe` exactly matches `src-tauri/target/release/inktile.exe`.
- Release SHA-256: `Inktile.exe` `A7181412197948919D68BFD0974AB02EF8ECB1DE2B31B3DF22CA1B7076BC5C4B`; portable ZIP `53BF15C68181E4AAE0143C12EC21A3DFC3BDF678DD829E06FA7ABFD4EFBD8FAB`; MSI `9E03431D9FAC34C4E821389427C04C9D2E22DB13C309BF274EEC00C0448C8043`; NSIS `3F2F131CE6C087B481055E9649355944C2D81B05DB0C98B132274AF34EC086EF`.

Updated on 2026-07-18 for Home settings, persistent application appearance, and combined library lookup/view ordering.

- Home settings: the editor theme button moved into an extensible Home Settings dialog with System/Light/Dark theme choices and 80%–120% UI scale. Both preferences are device-level, apply to Home and the editor, persist through reloads, and are no longer written into new `.inktile` manifests. Document zoom remains separate.
- Library lookup: Title was removed from View by. Title and text matches now share one result grid whose ordering follows the selected last-opened, creation-date, or last-edited mode plus ascending/descending direction; text occurrence counts remain visible.
- `npm run check`: passed after switching the installed Vite 8 build to its native config loader, avoiding an environment-owned `node_modules/.vite-temp` write while preserving the same production configuration. Documentation links, TypeScript/Vite build, archive round trip, and UI smoke syntax all passed.
- `CHROMIUM_PATH="C:\Program Files (x86)\Google\Chrome\Application\chrome.exe" npm run test:ui`: passed end to end in real Chrome. New coverage verifies the Settings dialog, theme and UI-scale persistence across a full reload, removal of the editor theme button, removal of Title view mode, and lookup reordering with Date created + ascending. The existing editor, drawing, geometry, persistence, browser save, native-mock save, and zero-console-error assertions also passed. The smoke bundle now uses `dist-smoke-current/` because the pre-existing `dist-smoke/` outputs are not writable by this sandbox identity.
- Visual review: the refreshed `inktile-preview.png` shows the Settings action integrated with the existing Home header and the three-option View by control at the final 100% UI scale.
- `npm run release:desktop`: passed its full `npm run check` preflight and Tauri frontend build, then failed when Rust could not write `src-tauri/target/release/.fingerprint/inktile-848fea544a04b592/bin-inktile` (`Access is denied`, OS error 5). Existing desktop artifacts were preserved and were not reported as refreshed.

Updated on 2026-07-18 for the complete Inktile product, archive, UI, documentation, icon, and desktop artifact rebrand. Earlier results remain below for regression context.

- Rebrand: product copy, TypeScript/Rust identifiers, CSS names, package/Tauri metadata, IndexedDB name, archive identity, and file extension now use Inktile/`.inktile`. The supplied horizontal mark leads `README.md`; the supplied square mark is used by the app, favicon, and regenerated native icon set.
- `npm run check`: passed (7 Markdown files, TypeScript/Vite production build, `.inktile` archive round trip, and UI smoke syntax).
- `npm run test:ui`: the first live attempt built the smoke bundle but failed because its default `/usr/bin/chromium` path is unavailable on this Windows machine. `CHROMIUM_PATH="C:\Program Files (x86)\Google\Chrome\Application\chrome.exe" npm run test:ui` then passed in real Chrome after the harness served the public logo and asserted decoded image pixels. Visual inspection of refreshed `inktile-preview.png` confirmed the Inktile mark and wordmark render on Home.
- `npm run release:desktop`: passed its full preflight and built `src-tauri/target/release/inktile.exe`, but the MSI phase failed at WiX ICE validation because this environment could not access the Windows Installer Service (`LGHT0217`/`LGHT0216`), so the script stopped before root artifact copying.
- `npm run tauri -- build --bundles nsis`: passed and produced `src-tauri/target/release/bundle/nsis/Inktile_0.1.0_x64-setup.exe`.
- MSI fallback: the already generated WiX object linked successfully with WiX `light.exe -sval` and workspace-local `WIX_TEMP`, bypassing only the unavailable ICE service; the resulting MSI was copied to `src-tauri/target/release/bundle/msi/Inktile_0.1.0_x64_en-US.msi`. Root `Inktile.exe` and `Inktile_0.1.0_windows_x64_portable.zip` were then refreshed from the final release binary.
- Release SHA-256: `Inktile.exe` `AFE5900DDA7942BA1E80D2B08B4C486443DF335F26137EF73793BFD390823ACE`; portable ZIP `75011CAD411C9DC6B091E0DA9508DA2ADE8597BC25339C66A1A098AF4013DF85`; MSI `962E63D5865F020BDEF7F6638984473B210BEBABE4557F34D54A4D4F3CD20A94`; NSIS `E73619F45157AEE276CEB89CFB9687D1C7C0DA30B9616C99570792A7C2BC2EB5`.
Updated on 2026-07-18 for native Save path semantics and refreshed desktop artifacts. Earlier 2026-07-18 library/performance and 2026-07-17 geometry results remain below for regression context.

- Native Save behavior: normal Save now overwrites a known external path, or confirms the existing IndexedDB library snapshot without opening a destination dialog when no external path exists. Save As remains the only path-choosing action. Loaded external/library records carry their path as part of the loaded-document value so callers cannot drop it accidentally.
- `npm run check`: passed after the final implementation and test updates (documentation checks, TypeScript/Vite production build, archive round trip with the native save-action matrix, and UI smoke syntax).
- `CHROMIUM_PATH="C:\Program Files (x86)\Google\Chrome\Application\chrome.exe" npm run test:ui`: passed in real Chrome. New Tauri-IPC-mock coverage verifies direct external open + Save overwrites the opened path without a dialog, a library reopen retains that path, normal Save on a library-only inktile does not create an external file, and explicit Save As opens the dialog and writes the chosen path. The existing browser/editor suite and zero-console-error assertion also passed. The sandboxed smoke build could not replace generated files (`Access is denied`), so the successful run used approved elevation.
- `npm run release:desktop`: the first approved run passed preflight but found stale generated Cargo metadata pointing at the old `inktile-editor-source` checkout. `cargo clean --release` removed only `src-tauri/target/release`; the rerun then passed the preflight, clean optimized Tauri build, MSI/NSIS bundling, root executable copy, portable ZIP recreation, and final hashing. Root `Inktile.exe` exactly matches `src-tauri/target/release/inktile.exe`.
- Release SHA-256: `Inktile.exe` `6D42DF445FD7066625C950742A5F67DFC9DC58CCD85EDB813DC11CB07477B4EB`; portable ZIP `A78CD80D7511B6311D280870E5082618AD404369C68567AD9F6CBB1FDB383FC0`; MSI `AB205BCA928F40C61C402EFAC3B2E3FBD057501C4FC27AB09750915CB4DD0634`; NSIS `00C126525E509D9D8CDF97BE3BC2CFD233128F3D0F61E788CD7350E6B9944FB6`.

- Repeated-transition performance: clean returns to Home now perform no archive persistence. Database version 3 keeps lightweight library metadata in `library-index`, so Home no longer reads every archived Blob and opening no longer rewrites the ZIP plus working snapshot merely to update `lastOpenedAt`. After an inktile is opened once, its working snapshot stays in the session cache for repeated opens. Existing version-2 catalog metadata migrates into the index during database upgrade without changing `.inktile` format version 1.
- `npm run check`: passed after the final implementation and documentation updates (7 Markdown files, TypeScript/Vite production build, archive round trip, database schema assertions, and UI smoke syntax).
- `CHROMIUM_PATH="C:\Program Files (x86)\Google\Chrome\Application\chrome.exe" npm run test:ui`: passed in real Chrome. Coverage includes version-2-to-3 IndexedDB migration, persisted first open, next-frame cached reopen, six consecutive clean Home/Open cycles all below the 750 ms regression ceiling, the full existing editor suite, save/download manifest checks, and zero browser console errors.
- Visual review: the passing suite refreshed `inktile-preview.png`; the home library rendered correctly with the fully edited seven-page inktile. The separate in-app browser runtime remained unavailable from the prior attempt (`Cannot redefine property: process`), so no in-app-browser pass is claimed.
- `npm run release:desktop`: passed with approved elevation after Inktile was closed. Preflight checks, optimized Tauri build, MSI and NSIS bundles, root executable copy, portable ZIP recreation, and final hashes completed. Root `Inktile.exe` exactly matches `src-tauri/target/release/inktile.exe`.
- Release SHA-256: `Inktile.exe` `D665A54BA7BEF02EA2246E523674B63E80BD417AE2BE586A96F5C8E14648A292`; portable ZIP `4026CC232023AD86E204D39D184C19D2582A5DF3673F1E93AF6702887E92A08C`; MSI `F5592721A7B0BCB3F6CD5549DABC982B2923581CDB2E8237486FCF563AB21C8E`; NSIS `EB12E55DFB32E40239F8024A8CC3948F046B90EAEDDE4B598DC922403075BEA8`.

- Home/library: startup now opens a local IndexedDB catalog of complete `.inktile` snapshots. New and externally opened inktiles enter the catalog; returning home flushes current edits; title edits rewrite the archived manifest; deletion clears only the local catalog copy and clears the recovery autosave only when it belongs to that document. A restart assertion confirms deleted recovery content is not re-imported. Existing `.inktile` format version 1 remains unchanged.
- Views and lookup: live coverage passed for last-opened, created, edited, and title view modes; ascending/descending switching; title matches rendered before text-only matches; and text-only results exposing occurrence counts in highest-frequency-first order.
- `npm run check`: passed after the final implementation and documentation updates (7 Markdown files checked, TypeScript/Vite production build, archive round trip, UI smoke syntax).
- `CHROMIUM_PATH="C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" npm run test:ui`: passed end to end with the smoke bundle served over loopback HTTP. The suite covered the new library lifecycle/search flow, deletion across reload, all existing editor interactions, browser download/save manifest checks, and return to the home library. `npm run test:ui` is now Windows-compatible because CSS copying uses `scripts/copy-smoke-css.mjs` instead of `cp`.
- Visual review: the passing suite refreshed `inktile-preview.png` on the home screen. The separate in-app browser-control runtime could not initialize in this environment (`failed to write kernel assets: The system cannot find the path specified`), so no in-app-browser run is claimed.
- `npm run release:desktop`: the first sandboxed attempt compiled the optimized app but stopped when WiX `light.exe` could not run. The approved elevated retry passed: preflight checks, optimized Tauri build, MSI and NSIS bundles, root executable copy, portable ZIP recreation, and final hashes all completed.
- Release SHA-256: `Inktile.exe` `9BBB3C7F2378CAA1000F71E265E3D6025A22CB6F8D68301237917F4B19807739`; portable ZIP `4BCC437BE255F39E7157F9DEE363A918E67245C3681C77ACBAEB17C8967A47D6`; MSI `EB6DA5E8A33B5BB48A5562303A0229D2B866A472F7E5281E2D72A2818A0363FA`; NSIS `9FD14015E70A8509F1C398B578BA4E72BD3F915B9354A2608E8A01B91ADB743F`.

- Rail top-offset and versions minimum-height refinements (third user-reported round): drawing and versions rails moved from 18px to 3px below the page-card top to match the left handle (mobile breakpoint aligned at 8px), and versions pages gained `MIN_VERSIONS_HEIGHT = 164` (3px offset + measured 158px rail + 3px breathing room) enforced in CSS and in the row-shrink clamp. Live Edge measurements: both rail offsets exactly 3.00px solo and grouped, versions card 164.00px at rest with the rail fully inside, notes face unchanged at 96px, shrink clamps at 164px, and a grouped versions+drawing row correctly uses the larger 240px drawing minimum. The updated smoke assertions were re-run verbatim against the live harness and passed.
- Zoom/stroke and versions-rail-anchor fixes (second user-reported round): drawing stroke rendering now multiplies line width by the live zoom factor (persisted widths stay zoom-invariant — verified in the saved manifest), and a zoom change triggers an immediate redraw. Live Edge measurements: on-screen stroke thickness 3.0px → 4.0px across a 100%→130% zoom (ratio 1.33). The versions rail now anchors to the top of its page card like the drawing rail (containing block moved to the card; 18px top offset measured for both rails in a tall grouped row; horizontal position unchanged). The full committed UI suite ran end-to-end to its final pass message over a secure localhost context in Edge.
- Rail positioning rework: external handles and rails are now positioned from the row's outer edges via `--inktile-row-width` and a per-cell `--inktile-cell-start` (the right cell's var updates live during a drag). Live Edge measurements over `http://127.0.0.1`: handle/rail drift during an in-flight drag fell from 209px to ≤0.3px; the versions rail at rest after an unequal drag sits +8.25px past the row's right edge (correct 8px gap, was +60.9px); the drawing rail remains at row edge + rail offset + 8px; a grouped image tracks its cell width within 1px during the drag; the equal-split default renders identically to the previous scheme. `min-width: 0` was added to `.page-wrapper`/`.page-card` to complete the media shrink chain.

- `npm run check`: passed on the combined tree (documentation checks for 7 Markdown files, TypeScript and Vite production build, `.inktile` archive round trip including the new `layoutWidthFraction` and legacy media `height` → `layoutHeight` cases, and `node --check scripts/ui-smoke.mjs`).
- `npm run test:ui`: the npm script itself cannot run on this machine — npm executes scripts through cmd.exe, where its `cp` step is not a valid command. The identical steps (esbuild bundle, CSS copy, `node scripts/ui-smoke.mjs`) were run in Git Bash with `CHROMIUM_PATH` pointed at Microsoft Edge because no Chromium is installed. The suite passed through empty startup, typography defaults, text caret/formatting/anchoring, and the versions section including the new single-column rail assertions, then failed at `supported media automatically creates a page` (2 !== 3). Root cause is environmental: the suite loads the app via `page.setContent`, which Edge treats as an insecure context, so `crypto.subtle` is undefined and the asset-hashing insertion path throws. The same failure reproduces on the unmodified code.
- Live secure-context verification (smoke bundle served over `http://127.0.0.1`, driven in Edge via playwright-core): column-boundary drag follows the cursor one-to-one at 110% zoom (60px cursor → 59.8px boundary) with the row width unchanged, minimum-width clamping without collapse, and undo restoring the equal split; image pages fill the page card exactly (identical bounding boxes) with no `.resize-handle` present, default height 420, shared row resize tracking one-to-one and clamping at the 120px media minimum; the versions rail renders as one 32px column outside the page's right edge with all six controls visible and functional.
- Known pre-existing Edge-environment smoke failures, unrelated to these changes: 1px borders report 0.909091px under 110% zoom; a stale late `.variant-toolbar` lookup after the versions page has been converted to text; the row-height one-to-one assertion reads 252 vs 260 due to the zoom-scaled handle center.
- Desktop fullscreen: `core:window:allow-set-fullscreen` added to `src-tauri/capabilities/default.json` (the `isFullscreen` getter was already covered by `core:default`). Runtime toggle behavior can only be confirmed in the native window.
- `npm run release:desktop`: passed. The preflight `npm run check` ran green again, the optimized Tauri build compiled (`inktile v0.1.0`, release profile, 1m 56s), MSI and NSIS bundles were produced under `src-tauri/target/release/bundle/`, root `Inktile.exe` and `Inktile_0.1.0_windows_x64_portable.zip` were refreshed from the release binary, and SHA-256 hashes were printed after the final copy and ZIP steps.
